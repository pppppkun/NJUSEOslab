#include <stdio.h>
#include "utils.h"
#include "data.h"
#include "func.h"

int main(){	
	int fileSize = 0;
	char filename[100];
	// scanf("%s", filename);
	format("../os.img");
	ls("../os.img", NULL, NULL);
}
